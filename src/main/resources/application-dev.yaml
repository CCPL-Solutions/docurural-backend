spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    co.edu.docurural: DEBUG
    org.springframework.security: DEBUG
    org.flywaydb: DEBUG
