spring:
  config:
    import: aws-parameterstore:/docurural/develop/
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

docurural:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}

logging:
  level:
    co.edu.docurural: DEBUG
    org.springframework.security: DEBUG
    org.flywaydb: DEBUG
